<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:showIn="@layout/activity_home" tools:context=".HomeActivity">

    <Button
        android:layout_width="250dp"
        android:layout_height="150dp"
        android:text="Жіночий Одяг"
        android:id="@+id/womensButton"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:textColorHighlight="@android:color/holo_purple"/>

    <Button
        android:layout_width="250dp"
        android:layout_height="150dp"
        android:text="Богдан"
        android:id="@+id/bogdanButton"
        android:layout_below="@+id/womensButton"
        android:layout_alignLeft="@+id/womensButton"
        android:layout_alignStart="@+id/womensButton" />

    <Button
        android:layout_width="250dp"
        android:layout_height="150dp"
        android:text="RoDICo"
        android:id="@+id/rodicoButton"
        android:layout_below="@+id/bogdanButton"
        android:layout_centerHorizontal="true" />
</RelativeLayout>
